# TODO

- [ ] Add/standardize ergonomic, colorful DataTables styling across all pages that render `table.datatable`.
- [ ] Ensure every table header cell (`<th>`) is automatically enhanced with icons via base template JS (already partially present).
- [ ] Ensure every table cell content gets a modern icon treatment (status chips, link eye icon, numeric badges) without breaking existing markup.
- [ ] Verify which templates include DataTables tables and ensure they use `class="datatable"` consistently.
- [ ] If any page uses a table without the expected class/wrapper, update that template to wrap with the existing `.datatable-wrapper` and use `.datatable`.
- [ ] Run Django server or sanity-check templates for HTML/JS errors.
- [ ] Commit changes.

